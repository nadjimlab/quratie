# سياسة الأمان — Qiraati

## الإبلاغ عن ثغرة

يرجى عدم نشر تفاصيل الثغرات الأمنية أو المفاتيح أو البيانات الشخصية في Issues العامة. أرسل وصفًا مختصرًا للثغرة، خطوات إعادة إنتاجها، وتأثيرها الأمني عبر قناة خاصة لمالك المستودع.

## قواعد حماية المساهمين

لا تُرفع مفاتيح Gemini أو Firebase أو أي رموز وصول إلى Git. يجب وضعها في Secrets الخاصة بـ GitHub Actions أو في ملف `.env` محلي غير متتبع. ملف `.env.example` يحتوي على أسماء المتغيرات فقط دون قيم سرية.

لا تُرسل صور الجداول أو بيانات التلاميذ إلى أي خدمة خارجية إلا بعد موافقة المستخدم ووفق سياسة الخصوصية. يجب تجنّب وضع أسماء التلاميذ أو أرقام الهواتف أو كلمات المرور داخل Issues أو سجلات البناء.

## البناء والنشر

يُنتج GitHub Actions نسخة Debug للاختبار فقط. لا يجوز استخدام مفتاح Debug لتوزيع نسخة إنتاجية. يجب إنشاء مفتاح Release محفوظ خارج المستودع، ثم ربطه بأسرار GitHub Actions عند تجهيز الإصدار النهائي.

## نطاق المسؤولية

Qiraati تطبيق مساعد للتنظيم والمتابعة الدراسية، ولا يُعد مصدرًا بديلًا عن البلاغات والوثائق الرسمية لوزارة التربية الوطنية الجزائرية.
