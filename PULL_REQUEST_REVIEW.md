# مراجعة Pull Requests المفتوحة

**تاريخ المراجعة:** 21 أغسطس 2026

راجعت Pull Requests المفتوحة في المستودع. جميعها Dependabot، وتعدّل ملفًا واحدًا فقط، وجميعها كانت بحالة `UNSTABLE` مع فشل فحص `Build and test APK` عند المراجعة. لذلك لم يتم دمج أي منها تلقائيًا قبل إعادة تشغيل الفحوصات على الفرع الحالي ومراجعة توافق الإصدارات الكبرى.

| PR | التغيير | الملفات | الحالة | القرار |
|---|---|---|---|---|
| #1 | تحديث Kotlin Coroutines Core من 1.10.2 إلى 1.11.0 | `gradle/libs.versions.toml` | فحص البناء فاشل | **تحتاج تعديل**؛ إعادة الفحص بعد تحديث المشروع، ثم الدمج إذا نجح الاختبار ولم تتغير واجهات coroutine المستخدمة. |
| #2 | تحديث Retrofit من 2.12.0 إلى 3.0.0 | `gradle/libs.versions.toml` | فحص البناء فاشل | **تحتاج تعديل**؛ هذا تحديث Major وقد يتطلب مراجعة API وConverter Moshi قبل الدمج. |
| #3 | تحديث `gradle/actions/setup-gradle` من 5 إلى 6 | `.github/workflows/android-build.yml` | فحص البناء فاشل | **تحتاج تعديل**؛ آمن مبدئيًا من ناحية الكود، لكن يجب إعادة تشغيل Workflow بعد تحديثه والتحقق من Java/Gradle cache. |
| #4 | تحديث OkHttp من 4.10.0 إلى 5.5.0 | `gradle/libs.versions.toml` | فحص البناء فاشل | **تحتاج تعديل**؛ تحديث Major يستلزم مراجعة Retrofit وواجهات OkHttp المستخدمة في فحص الإصدار. |
| #5 | تحديث `actions/checkout` من 5 إلى 7 | `.github/workflows/android-build.yml` | فحص البناء فاشل | **تحتاج تعديل**؛ يجب التحقق من توافق Node runtime مع بقية Actions قبل الدمج. |
| #6 | تحديث Retrofit Moshi Converter من 2.12.0 إلى 3.0.0 | `gradle/libs.versions.toml` | فحص البناء فاشل | **تحتاج تعديل**؛ مرتبط مباشرة بـ PR #2 ويجب اختبار الاثنين معًا أو اختيار تحديث متوافق. |
| #7 | تحديث Room Runtime من 2.7.0 إلى 2.8.4 | `gradle/libs.versions.toml` | فحص البناء فاشل | **تحتاج تعديل**؛ يجب تحديث Room KTX وCompiler معًا إن لزم والتحقق من مخطط قاعدة البيانات والاختبارات. |

## التوصية

لا أوصي بالرفض النهائي لأي PR أمنيًا؛ التحديثات مفيدة، لكن PRs #2 و#4 و#6 تغييرات Major وتحتاج اختبارًا منفصلًا. الأولوية العملية هي إعادة تشغيل PRs #1 و#3 و#5 بعد الإصلاحات الحالية، ثم اختبار مجموعة Room، ثم مراجعة حزمة Retrofit/Converter/OkHttp معًا. يجب دمج PR واحد في كل مرة، وتشغيل `testDebugUnitTest` و`assembleDebug` و`connectedDebugAndroidTest` بعد كل عملية دمج.

لم يتم تعديل أو إغلاق أو دمج أي Pull Request أثناء هذه المراجعة، حفاظًا على قرار المالك ومفتاح التوقيع الحالي.
